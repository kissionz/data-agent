# PRD 验收覆盖矩阵

> 阶段：核心 UI、业务服务与 BFF、真实 PostgreSQL 只读查询、事务型 durable query runtime
> 日期：2026-07-23
> 口径：只把已经在仓库中可运行、可检查、可测试的内容标为“已覆盖”。需要真实后端、权限系统、数据源、模型或评测平台的要求标为“部分覆盖”或“未覆盖”。

## 总体结论

当前阶段已经完成 ChatBI 核心体验的可视化、身份/工作空间/策略服务契约、数据源治理入口与服务契约、语义治理入口与服务契约、导出分享治理契约、协作资产入口与服务契约、评测回放入口与门禁契约、模型运营路由契约、SLO 报告与性能预算契约、开发者接入治理契约、领域状态机、共享契约包源码、本地编译执行边界、API app 壳和本地 BFF router 基座，足以作为产品评审、前后端契约拆分和后续 API 开发的起点。完整 PRD 的生产验收尚未完成，尤其是真实 OIDC/SAML/SCIM、真实数据源、审计存储、线上评测流水线、真实模型网关和真实 SLO 证明仍需后续阶段实现。

## 功能覆盖

| PRD 功能域 | 当前状态 | 已覆盖内容 | 仍需补齐 |
|---|---|---|---|
| F01 身份、租户与工作空间 | 部分覆盖 | UI 展示工作空间、业务域、用户角色与上下文；领域 mock 保留权限输入；本地 BFF 通过请求头模拟 actor 并测试跨 workspace 拒绝；`apps/api` 在非 local 环境默认要求 header actor 上下文；新增 `IdentityPolicyApplicationService` 与 `/v1/identity/*` API，覆盖身份上下文、可见工作空间、RBAC/ABAC 策略裁决、受限导出拒绝、策略版本、权限摘要、缓存 scope 和策略更新后 300 秒内生效/旧缓存失效；F11 开发者接入已提供项目内 API Key 与 service-account actor；conversation/run 持久化端口、内存 adapter、本地 JSON 文件 adapter、SQL migration 与可替换 SQL adapter 已覆盖。 | 真实 OIDC/SAML、SCIM、OIDC Client Credentials/用户委托短期访问令牌、生产租户/组织/工作空间持久化、外部 Policy Engine、策略审批和具体数据库 driver 接入；F11 的项目内开发者 API Key 不等同于上述企业身份令牌。 |
| F02 数据源与元数据 | 部分覆盖 | 数据源中心页面；数据源健康度、搜索/状态筛选、连接测试反馈、元数据目录、字段分类、样本策略、质量门禁和同步记录；新增 `DataSourceApplicationService` 与 `/v1/data-sources` API，覆盖 actor 可见范围过滤、只读 credential ref、不暴露真实凭据、连接测试、质量门禁状态、元数据目录和受限字段样本策略；`GET /v1/data-sources/{id}/lineage` 已提供上游来源、下游认证指标/看板/验证案例、字段级引用和影响摘要，`POST /v1/data-sources/{id}/schema-review` 已覆盖 Schema 变更审批门禁，删除影响 P0 认证指标的字段会阻断，低风险新增字段可批准，非管理员不可评审；组件测试和服务/API 测试覆盖关键规则。 | 真实数据源连接器、凭据校验、元数据扫描任务、真实血缘采集、枚举采样执行器、质量门禁执行器、Schema 变更审批流落库和数据源持久化。 |
| F03 语义层与指标治理 | 部分覆盖 | 语义中心页面；指标列表、公式、维度、依赖、版本历史、审批状态；语义版本领域模型；本地 Semantic Catalog 覆盖认证指标、草稿指标、维度、兼容维度、语义版本边界和 Join Graph 风险门禁；新增 `SemanticGovernanceApplicationService` 与 `/v1/semantic/metrics` API，覆盖指标列表/详情、提交评审、自动参考 SQL 对账结果、对账失败阻断认证、对账通过后认证发布、角色权限、不可变版本、Join Graph 风险暴露、灰度发布计划、5/20/50/100 发布阶段、自动回滚阈值、语义指标回滚和 public audit event。 | 真实语义对象持久化、Join Graph 编辑审批流、真实参考 SQL 执行器、真实灰度流量切换、版本仓库回滚执行和认证口径 100% 生产证明。 |
| F04 理解、检索与规划 | 部分覆盖 | mock 场景覆盖标准查询、澄清、失败、越权和部分结果；领域状态机限制展示状态；新增 `AnalysisIR v1` 契约和 deterministic service；`PublicRunView` 已携带本地 retrieval trace 与 planner trace，覆盖原问题规范化、租户/工作区/业务域/语义版本权限过滤、实体链接候选、未授权候选不外露、实体链接 F1/词法覆盖目标、Planner 多步计划、每步输入/输出/预算/依赖/终止条件和可回放检索策略版本。 | 真实向量/全文检索服务、真实实体链接模型、Planner LLM/工具编排、黄金问题自动评测流水线和线上 F1/通过率证明。 |
| F05 澄清与多轮会话 | 部分覆盖 | 澄清 UI 与候选选择流程；本地 application service 和 BFF router 绑定原 run、candidate id 和 candidate version；会话 active run 阻断已测试；durable SSE 使用最长 25 秒 finite long-poll、持久化 sequence、Last-Event-ID、无事件 heartbeat 和 Node disconnect AbortSignal，总 deadline 覆盖事件存储读取，PostgreSQL 读取使用 dedicated client 与独立 cancellation pool 执行 `pg_cancel_backend`，断连清理 timer/listener 且不回退内存；澄清候选版本失效时服务端会重新鉴权并刷新候选版本，保持未执行查询并提示重新澄清，不静默选择旧候选。 | 真正 push 式 SSE 长连接/LISTEN-NOTIFY、真实多轮状态持久化、跨业务域追问的结构化状态迁移、候选失效的实时通知和持久化会话版本仓库。 |
| F06 编译、查询与安全执行 | 部分覆盖 | 本地确定性 Compiler 先通过 Semantic Catalog / Join Graph 校验 metric/dimension ID、版本、认证状态、粒度、兼容维度和 Join 风险，再将 Analysis IR 编译为只读 SQL AST/SQL；计划已绑定服务端治理的 `dataSourceId` 和确定性输出 schema。Query Gateway 注入租户、工作区、业务域守卫并生成 public-safe 摘要；Node-only PostgreSQL adapter 在同一 `BEGIN READ ONLY` 事务内执行参数化 JSON EXPLAIN、真实预算阻断和查询正文，设置 statement/idle transaction timeout，并用独立连接 `pg_cancel_backend` 传播 AbortSignal。生产 PostgreSQL runtime 已使用独立 warehouse/control-plane credential reference、连接池与 TLS 策略；`POST /v1/questions` 仅在 Run、Conversation、幂等预留、审计和 Job 同事务提交后返回 202。取消、complete/fail/retry、Result manifest/page、审计和事件序列由 attempt/lease/fence/token/expiry 门禁在同一事务提交；cancel-vs-complete 单赢家，晚到 lease 被拒绝，事件失败整事务回滚。Worker host 提供 single-flight、heartbeat、跨进程取消观察、readiness 和排空关闭，超过 drain deadline 会主动 abort 当前查询；SIGINT/SIGTERM 同时停止 HTTP admission 与启动 runtime drain，重复信号/关闭幂等，后台清理有界且不持有事件循环。Reconciler 使用相同 Job→Run→Conversation 锁序与 fence/attempt CAS，只修复可证明安全的终态 Conversation/失效 Job，其余 finding 通过 canonical identity + SHA-256 去重持久化；001–005 migration CLI 使用非阻塞 advisory lock、SHA-256 ledger、连续前缀校验、schema drift 防线和逐文件事务。 | 仍需 durable outbox、Snowflake/其他方言 adapter，以及在可用 PostgreSQL 环境执行已落盘的真实并发/回滚/reconciler/停机 integration；生产压测、故障注入和编排器最终退出策略证据尚未完成。 |
| F07 答案、图表与证据 | 部分覆盖 | 结论、KPI、趋势图、表格、证据、默认条件、口径、来源、新鲜度、语义版本；真实 Query Adapter rows 已通过编译期输出 schema 映射为 `RunResult`，严格拒绝字段漂移/重复列/rowCount 不一致，支持 numeric 高精度字符串、日期、null、boolean、空结果与截断结果；答案标记 `generatedFrom=query_result`，事实值逐项引用真实结果单元格，完成前继续由 `validateResultGrounding` 和 chart spec validator 校验。Worker 使用真实 SHA-256 生成 result/page/manifest identity，并把 immutable pages 与 manifest 和最终 Run 原子发布；Result API 重新校验 Run/Conversation/domain，只读取已发布 manifest 的 offset/limit 相交页并校验 attempt/page/checksum/rowCount/columns，staged page 不可见；SSE 从持久化 sequence 按 Last-Event-ID 恢复。 | Queue terminal JSON 仍保留结果 publication 摘要，后续需迁移为纯 manifest reference；还需对象存储/缓存或流式大结果、生产级图表误导性校验 worker、derived/transform fact 的可审计计算链和 schema 演进。 |
| F08 导出、分享与嵌入 | 部分覆盖 | UI 上预留导出/分享动作和上下文约束展示；工作台 CSV 下载接入本地导出治理服务，文件写入水印、策略版本、权限摘要、脱敏规则和审计事件；协作资产中心展示分享范围、导出水印/脱敏审计策略和重新鉴权提示；新增 `SharingExportApplicationService` 与 `/v1/sharing/*` API，覆盖导出前重新鉴权、100k 行/50MB 在线限制、受限分类阻断、水印计划、脱敏规则、短期下载链接预览、超阈值导出进入受审计异步队列、`GET /v1/sharing/exports/{id}` 状态查询、异步 worker 将 queued 导出推进为 completed、对象存储制品清单、checksum、watermarkApplied、短期下载预览、通知 payload 不含下载链接、分享只保存引用不复制结果、接收者重新鉴权和 public audit event。 | XLSX/PDF/PNG 真实二进制渲染、真实对象存储 adapter、生产水印写入、嵌入式 SDK、短期 embed token 和生产通知通道。 |
| F09 运营、模型与监控 | 部分覆盖 | 运营中心页面；SLO、门禁、失败分布、回放队列、模型版本、延迟趋势；`EvaluationApplicationService` 暴露当前候选版本黄金集门禁和失败回放列表，支持按状态/业务域/关键词过滤；新增 `ModelOpsApplicationService` 与 `/v1/model-ops/*` API，覆盖模型能力路由、active/candidate 版本、超时、温度、租户覆盖、配额检查、供应商不可用/配额不足/策略阻断降级链、灰度候选选择、发布门禁阻断和运维/安全管理员回滚；新增 `SloApplicationService` 与 `/v1/operations/slo*` API，覆盖 SLO 报告、P95 延迟、成本、取消传播、扫描量预算、错误预算、告警 runbook、public audit 和 allow/warn/block 性能预算决策。 | 真实监控事件、真实 Model Gateway 调用、成本采集、调用审计落库、自动回滚阈值执行、告警投递、真实压测证明与按租户/业务域/模型版本的线上指标下钻。 |
| F10 评测、审计与回放 | 部分覆盖 | 领域测试覆盖状态、安全、语义版本；PublicRunView 携带审计事件；审计事件可通过 persistence 端口列出，可由本地 JSON 文件 adapter 落盘，并在 SQL adapter 中单独落入 `chatbi_audit_events` 表，且 migration 包含 run/scope 审计查询索引；retention cleanup planner/executor 已覆盖租户/工作区级默认留存：问题/IR/SQL 指纹 180 天、结果摘要 30 天、原始结果 7 天、敏感样本 3 天、审计 365 天，并执行 tenant/workspace scoped SQL 清理语句；`FeedbackApplicationService`、`POST /v1/feedback` 与工作台反馈表单已覆盖点赞/点踩、原因标签、备注、可选正确答案和问题上报，反馈重新校验源 Run 可见性，关联 run/request/trace/语义版本，手机号/邮箱/证件号先脱敏且不携带生产结果明细；运营中心已拆为总览/黄金集/回归运行/失败回放四个任务页签，黄金集 UI 覆盖搜索、状态/业务域/场景筛选、质量门禁、详情检查器、审批说明、批准、已批准样本选择和回归调度；评测服务与 `/v1/evaluation/*` API 覆盖黄金样本列表/详情、回归计划列表/详情、候选集→黄金集审批、计划持久到服务实例内存、查看/采集/审批/调度角色分离、审批备注必填/限长/脱敏、租户/工作空间分别隔离、跨作用域 ID 统一不可见、无效回归范围整体拒绝且样本 ID 去重；基于 5 项本地 fixture 指标形成 deterministic 发布阻断契约，并提供失败回放详情、需要脱敏且不使用生产凭据的回放计划契约。 | 第 14 章缺失的 Semantic Accuracy、Groundedness、Security Accuracy 真实计算与门禁；覆盖多轮、越权、复杂 Join、故障的黄金样本；质量门禁扫描/去重/人工标注制品；反馈/黄金集/回归计划真实持久化；版本变更/发布自动触发、真实批量回归队列/worker 与 queued→running→terminal 状态推进、真实版本差异、真实脱敏与失败回放执行、影子/灰度/回滚联动、发布阻断落库和生产清理调度。 |
| F11 开放 API / SDK | 部分覆盖 | `apps/api` 提供 API app 壳、运行时配置、`/readyz`、header actor guard、Bearer API Key 验签注入 service-account actor、memory/file persistence mode；`@insightflow/contracts` workspace 包已拥有独立 `api`、`domain`、`events`、`openapi`、`sdk` 源码与子路径导出，应用可通过包名消费共享契约、OpenAPI 草案、endpoint scope 映射、Bearer 请求构造 helper、typed endpoint 请求 helper、embed iframe 配置/snippet helper 和数据库凭据扫描护栏，`src/contracts`/`src/api/openapi.ts` 仅保留兼容层；SDK helper 已覆盖问题提交、反馈提交、Run 读取、SSE、结果分页、导出创建、导出状态查询，反馈使用独立 `feedback:write`、导出状态使用独立 `exports:read` 最小权限 scope；OpenAPI 已包含 `PublicApiError`、`ErrorEnvelope`、通用 `ApiEnvelope`、`PublicRunEnvelope`、`ResultPageEnvelope`、`FeedbackView` 和关键 Run/Result/Question/Feedback 路径的统一 JSON/error response 引用；新增 `DeveloperAccessApplicationService` 与 `/v1/developer/*` API，覆盖服务账号、API Key 签发/撤销/轮换/验签、scope、配额递增、过期、轮换宽限期、workspace/domain 边界、服务端可信 actor 生成、密钥 hash/脱敏预览、Webhook HTTPS/HMAC 签名/重放保护/完整退避计划/重试/死信计划、Webhook delivery BFF 路由与 OpenAPI schema、Webhook delivery queue/http client 端口与本地 dispatcher、短期 embed token 和“组件不能接触数据库凭据”；本地 BFF router 覆盖 `/healthz`、`/openapi.json`、身份上下文/策略裁决、开发者接入治理、问题提交、反馈、Run 查询、结果分页、SSE 事件、澄清、取消、数据源列表/详情/连接测试、语义指标列表/评审/认证、导出/分享治理、评测门禁/回放、模型运营路由/决策/回滚、SLO 报告/性能预算评估、协作资产列表/收藏/订阅/审计；错误码目录已覆盖全部 public code。 | 服务账号令牌轮换任务、Fastify/TypeBox 生产 OpenAPI 生成、SDK 代码生成与包发布、真实 Webhook 队列/HTTP client adapter、调用审计落库、API Key 生产级存储加固；OpenAPI 仍需从 schema 生成产物升级为生产级规范，并继续补齐全部业务 DTO/事件 payload schema。 |
| F12 协作资产与订阅 | 部分覆盖 | 协作资产中心页面；资产库、搜索/状态筛选、收藏反馈、归档状态、订阅反馈、分享范围、审核人、版本快照、审计事件；新增 `CollaborationAssetApplicationService` 与 `/v1/assets` API，覆盖 actor 可见范围过滤、收藏更新、重命名权限、审核中/归档不可订阅、订阅更新、接收者重新鉴权摘要、订阅通知计划、水印/明细行排除护栏和 public audit event；短期 embed token 已通过开发者接入服务覆盖嵌入式读取边界；组件测试和服务/API 测试覆盖关键规则。 | 真实资产持久化、权限分享链接、真实通知发送、生产订阅调度、协作权限策略落库、真实嵌入式 SDK 包和导出水印文件生成。 |

## UI 验收覆盖

| 项目 | 当前状态 | 证据 |
|---|---|---|
| 桌面四栏布局 | 部分覆盖 | `src/App.tsx` 与 `src/styles.css` 已实现 68px / 260px / main / 235px 布局；仍需 1440px 浏览器尺寸断言与无遮挡截图证据。 |
| 固定底部输入 | 部分覆盖 | 工作台主区已实现固定 composer；新增反馈表单会自动滚入可见区，并已在 1280×720、390×844 浏览器视口检查。仍需移动软键盘、长结果滚动和底部 Sheet 同时打开的自动化证据。 |
| 状态无歧义 | 已覆盖 | 领域状态与 UI 标签只使用六种展示状态；部分结果不作为第七状态。 |
| 约束可见 | 已覆盖 | 结果和右侧上下文显示业务域、时间、过滤、指标、来源、语义版本。 |
| 澄清候选 | 已覆盖 | 澄清卡片展示候选口径和选择动作。 |
| 权限拒绝 | 已覆盖 | 越权场景返回安全拒绝，不展示候选值或无权资源名称。 |
| 图表替代表格 | 已覆盖 | 结果支持图表、表格、证据三种 tab。 |
| 响应式 | 部分覆盖 | CSS 覆盖桌面、平板抽屉、移动单栏与底部面板；Playwright 覆盖 Desktop Chrome/Pixel 5，本轮人工检查 1280×720 与 390×844。仍缺 1440/1280 边界尺寸断言、平板关键断点、WebKit/Safari 和移动软键盘证据。 |
| 可访问性基础 | 部分覆盖 | 表单、按钮、状态文本、图表替代表格、tablist、dialog、焦点可见样式和 reduced-motion 已处理；`src/test/accessibility.test.tsx` 已覆盖关键导航、键盘提交、焦点可达、状态非颜色依赖、图表表格替代和命名 dialog。仍需真实浏览器屏幕阅读器与跨断点键盘 E2E。 |

## 自动化验证覆盖

| 验证项 | 当前状态 |
|---|---|
| TypeScript 项目检查 | 已覆盖，本轮于 2026-07-23 运行项目约定的 `npm run build`（包含 `tsc -b` 与 Vite production build）通过。 |
| 领域状态机测试 | 已覆盖，`src/test/run.test.ts`。 |
| 权限与安全拒绝测试 | 已覆盖，`src/test/security.test.ts`。 |
| 语义与模型测试 | 已覆盖，`src/test/models.test.ts` 验证语义指标、Semantic Catalog、版本边界和 Join Graph 风险门禁。 |
| 契约测试 | 已覆盖，`src/test/contracts.test.ts` 验证未知字段拒绝、IR schema 与安全护栏；`src/test/contractsPackage.test.ts` 验证 `@insightflow/contracts` 包入口和 `api`/`domain`/`events`/`openapi`/`sdk` 子路径导出版本、schema、错误码、公共状态词表、SSE helper、Bearer security scheme、Webhook delivery schema、公共错误包络 schema、Run/Result envelope schema、SDK endpoint helper、数据库凭据扫描护栏和 OpenAPI 路径。 |
| 应用服务测试 | 已覆盖，`src/test/application.test.ts` 验证提交、幂等、澄清、越权、跨工作空间拒绝。 |
| 本地编译/网关测试 | 已覆盖，`src/test/query.test.ts` 验证确定性 SQL 指纹、语义 Catalog 校验、Join Graph 拒绝、权限守卫注入、参数化过滤、只读拒绝、预算阻断和 public-safe 执行摘要。 |
| 本地 BFF/API 测试 | 已覆盖，`src/test/api.test.ts` 验证健康检查、OpenAPI、HTTP 状态码、CORS、幂等、澄清和取消；`src/test/apiRuntime.test.ts` 验证 `apps/api` readiness、header actor guard、Bearer API Key auth、scope 拒绝和 file persistence runtime；`src/test/feedbackService.test.ts` 验证负反馈原因门禁、源 Run 重新鉴权、敏感文本脱敏、链路关联、不携带生产结果明细和 `/v1/feedback`；`src/test/identityPolicyService.test.ts` 验证身份策略服务与 `/v1/identity/*` API；`src/test/developerAccessService.test.ts` 验证服务账号、API Key 签发/撤销/轮换/验签、轮换宽限期、scope、配额、边界、Webhook 签名投递计划/完整 queued 退避计划/dispatcher 重试/死信/不泄漏 payload、embed token 和 `/v1/developer/*` API；`src/test/dataSourceService.test.ts` 验证数据源服务、字段级血缘、Schema 变更审批和 `/v1/data-sources` API；`src/test/semanticGovernanceService.test.ts` 验证语义治理服务与 `/v1/semantic/*` API；`src/test/sharingExportService.test.ts` 验证导出分享服务与 `/v1/sharing/*` API；`src/test/evaluationService.test.ts` 验证评测门禁、黄金样本列表/详情/筛选、查看/采集/审批/调度角色分离、审批备注脱敏、同租户跨工作空间与跨租户隔离、无效范围整体拒绝、样本去重、回归计划存储/列表/详情及 `/v1/evaluation/*` 路由；`src/test/modelOpsService.test.ts` 验证模型运营路由、配额、降级、门禁、回滚和 `/v1/model-ops/*` API；`src/test/sloService.test.ts` 验证 SLO 报告、告警 runbook、性能预算 allow/warn/block 和 `/v1/operations/slo*` API；`src/test/collaborationService.test.ts` 验证协作资产列表、收藏、重命名、订阅、通知计划、审计和 `/v1/assets` API。 |
| SSE/错误码契约测试 | 已覆盖，`src/test/events.test.ts` 验证 public error catalog、Run 事件序列和 Last-Event-ID 过滤。 |
| 持久化端口测试 | 部分覆盖，`src/test/persistence.test.ts` 验证跨 service 实例读取、作用域幂等键、clone 防引用污染、audit list、本地 JSON 文件恢复、SQL migration、versioned migration runner、基于 fake/in-memory SQL executor 的 adapter 恢复与审计落表合约、审计 scope 索引、retention 默认天数、敏感样本短留存、审计长留存、tenant/workspace scoped 清理 SQL 和 cleanup executor；`resultStore.test.ts` 覆盖 staged page、manifest publish、checksum collision、event CAS 和 async port；2026-07-23 独立 PostgreSQL adapter/migration 单元门禁共 6 个文件 68 项通过，默认 Vitest 47 个文件 303 项通过，并准备真实 PostgreSQL integration。真实数据库执行仍未在当前 Docker 环境完成。 |
| 组件级 UI 测试 | 部分覆盖，已覆盖所列页面的关键状态：`src/test/workbench.test.tsx` 覆盖工作台默认结果、约束可见、表格替代、结构化负反馈/正确答案/问题上报、澄清和权限安全失败；`src/test/semanticGovernance.test.tsx` 覆盖语义中心指标定义、筛选、编辑和审批；`src/test/operationsCenter.test.tsx` 覆盖运营中心四任务页签、键盘切换、黄金样本详情/质量门禁/审批、选择样本、回归调度、SLO、发布门禁、模型版本、失败分布、回放详情和刷新反馈；`src/test/dataSources.test.tsx` 覆盖数据源中心关键状态；`src/test/collaboration.test.tsx` 覆盖协作资产关键状态。身份设置、完整回归结果等 PRD 页面/状态仍未覆盖。 |
| 浏览器人工验收 | 部分覆盖且证据有限；2026-07-02 已用本地浏览器核验反馈表单在 1280×720 与 390×844 视口的展开、自动滚入可见区、脱敏上报和状态回执。过去主工作台、澄清、权限拒绝、语义中心、运营中心等人工检查没有仓库内截图/报告，不作为强完成证据。 |
| 浏览器自动 E2E | 部分覆盖，`tests/e2e/prd-acceptance.desktop.spec.ts` 和 `tests/e2e/prd-acceptance.mobile.spec.ts` 使用 Playwright + 本机 Chrome 覆盖标准查询结果、表格替代、口径证据、CSV 文件下载与水印/策略/审计元数据、结构化负反馈/正确答案/敏感文本脱敏/问题上报、澄清、权限拒绝、运行中取消、部分结果显式提示、刷新后恢复上次分析结果、黄金候选样本审批、已批准范围选择、回归调度及 queued 0/5 阶段展示、语义指标评审/认证、数据源降级质量门禁、受限字段样本策略、协作资产重新鉴权/水印/订阅阻断、运营回放详情和移动端会话/上下文面板；2026-07-23 Chrome 13 项全部通过。仍需扩展 WebKit/Safari、真实 XLSX/PDF/PNG 文件、异步大文件导出轮询和黄金治理完整移动端交互路径。 |
| 可访问性 E2E | 部分覆盖，`src/test/accessibility.test.tsx` 在 jsdom 中覆盖键盘提交、焦点、ARIA 角色、命名 dialog、tablist、图表替代表格和状态文本；Playwright 已覆盖命名按钮/对话框/移动面板可达，仍需屏幕阅读器级真实浏览器 E2E。 |
| 性能与 SLO | 部分覆盖，`SloApplicationService` 与 `/v1/operations/slo*` 已覆盖本地 SLO 报告、P95 延迟、单次成本、取消传播、扫描量预算、告警 runbook 和预算决策测试；仍需真实 API 流量、数据源执行指标、监控事件、压测和告警投递证明。 |

## 下一阶段验收门槛建议

1. 将 `@insightflow/contracts/openapi` 从当前草案升级为 schema 生成产物，并把 `@insightflow/contracts/sdk` endpoint helper 手写基线升级为 SDK 代码生成/发布流程。
2. 将 SQL persistence 端口接入具体 SQLite/PostgreSQL driver，并补连接池配置、迁移执行 CLI、生产审计查询 API 和 retention cleanup 调度器。
3. 将当前 `apps/api` Node adapter 替换为 Fastify/TypeBox，补 OIDC/API key 中间件、生产 SSE 长连接和错误码表落地文档。
4. 继续扩展可重复浏览器测试：WebKit/Safari、真实 XLSX/PDF/PNG 文件生成和异步大文件导出轮询。
5. 任何真实模型接入前，答案必须保留“只能引用结果集或授权知识”的测试护栏。
